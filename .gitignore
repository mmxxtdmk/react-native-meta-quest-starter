# =====================================================
# Bespoke .gitignore for react-native-meta-quest-starter
# =====================================================
# Primary purpose: Jupyter Notebook starter for React Native + Meta Quest 3
# GitHub Pages lives at /docs/index.html → DO NOT ignore /docs/
# Keeps repo clean for public OSS sharing (Feb 2026 React Native Quest launch)
# =====================================================

# === Virtual Environment (used for nbconvert to GitHub Pages) ===
venv/
.venv/
env/
ENV/
__pycache__/
*.pyc
*.pyo
*.pyd

# === Jupyter Notebook temporary / checkpoint files ===
.ipynb_checkpoints/
**/.ipynb_checkpoints/
*.nbconvert.html

# === Node.js / Expo / React Native dependencies & caches ===
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log
.expo/
.expo-shared/
dist/
web-build/
eas-build-local.log
expo-env.d.ts

# === Android / Meta Quest native build artifacts (prebuild) ===
# Keep the android/ folder structure for reference, but ignore heavy outputs
android/.gradle/
android/app/build/
android/build/
android/local.properties
android/.idea/
*.keystore
*.jks
*.p8

# === Environment variables & secrets (never commit) ===
.env
.env.local
.env.*.local
local.properties

# === Build / Coverage / Temp folders ===
build/
coverage/
.nyc_output/
tmp/
temp/
*.tmp
*.temp

# === OS-generated files (Windows + macOS) ===
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# === Editor / IDE files (partial – keep useful VS Code config) ===
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/tasks.json
.idea/
*.swp
*.swo

# =====================================================
# WHAT YOU SHOULD COMMIT (explicitly allowed):
#   • Your main .ipynb notebook file
#   • README.md
#   • docs/index.html (and entire /docs/ folder)
#   • Any assets/, src/, App.tsx, app.json you include
# =====================================================